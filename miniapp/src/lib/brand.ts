/**
 * Фирменные цвета приложения.
 *
 * В отличие от --tg-theme-*, это не палитра клиента Telegram, а собственный
 * акцент продукта — он не должен зависеть от того, какую тему выбрал
 * пользователь. Используется и в CSS (styles/theme.css, те же значения),
 * и здесь — там, где цвет нужен в JS: MainButton раскрашивает Telegram
 * нативно, через API, а не через CSS.
 */
export const BRAND = {
  rose: '#D6336C',
  honey: '#E4A11B',
  pine: '#1B7A6C',
  white: '#ffffff',
} as const;
